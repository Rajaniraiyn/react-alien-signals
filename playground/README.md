# React Alien Signals playground

The playground links the package from the repository root and runs it in a
React 19 Vite application.

From the repository root:

```bash
bun install
bun run --cwd playground dev
```

Build it with `bun run --cwd playground build`.
